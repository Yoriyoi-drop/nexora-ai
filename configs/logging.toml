# Logging Configuration

[general]
level = "info"
format = "json"
enable_colors = false

[file]
enable = true
path = "/var/log/nexora"
rotation = "daily"
max_size_mb = 500
max_files = 30

[console]
enable = true
level = "warn"

[tracing]
enable = true
sample_rate = 0.05