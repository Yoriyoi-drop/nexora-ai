[build]
# Sccache: cache kompilasi lintas sesi (butuh `cargo install sccache`)
# rustc-wrapper = "sccache"
# Target dir di ramdisk/tmp untuk SSD lifespan
# target-dir = "/tmp/nexora-target"
incremental = true

[env]
# Optimalisasi untuk CPU saat ini
# CARGO_BUILD_RUSTFLAGS = "-C target-cpu=native"
