[package]
name = "nexora-model-kronos"
version = "0.1.0"
edition = "2021"
description = "NXR-KRONOS Model"

[dependencies]
nexora-model-core = { path = "../model-core" }
nexora-shared = { path = "../shared" }
nexora-transformer = { path = "../transformer" }
nexora-cognition = { path = "../cognition" }
nexora-memory = { path = "../memory" }
nexora-hallucination = { path = "../hallucination" }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
ndarray.workspace = true
rand.workspace = true
tracing.workspace = true
async-trait.workspace = true
tokio = { workspace = true, features = ["rt", "macros"] }
uuid = { workspace = true, features = ["v4"] }
chrono = { workspace = true, features = ["serde"] }
futures.workspace = true

[features]
default = []
gpu = ["nexora-model-core/gpu", "nexora-shared/gpu", "nexora-cognition/gpu"]
cuda = ["nexora-model-core/cuda", "nexora-shared/cuda"]

